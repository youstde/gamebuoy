var kitty = require('vkitty');
var server = require('../index.js');
kitty.watch('./pages/*/index.html')
    .pipe(server.src({port:8080}))
    .pipe(server.cdn({port:8083,headers:{"power":"vkitty.org"}}))
